import Navbar from "../components/Navbar";

const Home = () => {
  return (
    <div>
      <Navbar />
      <div>Nothing to see here for now</div>
    </div>
  );
};

export default Home;
